namespace SlLib.Resources.Model;

public enum SlVertexElementType
{
    UByte = 1, // Don't think this is what this is, but it's not used in any model files, so it's probably safe
    UByteN = 2,
    Half = 4,
    Float = 5
}